<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mappers.order">
	
	<select id="selectNowBuy" parameterType="com.myspring.tibet.order.vo.OrderVO">
		select o.*, c.product_name, p.product_sale, p.product_color, p.product_size, p.product_thumbnail, p.product_count
		    from tibetdb.product as p
		    join tibetdb.order as o
		    on o.product_num = p.product_num
	</select>
	
</mapper>