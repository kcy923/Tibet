<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mappers.order">
	
	<insert id="addOrder" parameterType="java.util.Map">		<!-- com.myspring.tibet.order.vo.OrderVO -->
		insert into cart(order_name, order_tel, order_phone, order_email, 
			receiver_name, receiver_zip, receiver_addr1, receiver_addr2, receiver_tel, receiver_phone, receiver_msg, 
			order_state, order_pay, product_num, product_name, product_thumbnail, product_price, product_sale, product_color, product_size, product_count, user_id) values
		<foreach collection="addList" item="item" separator=" , ">
			(#{item.order_name}, #{item.order_tel}, #{item.order_phone}, #{item.order_email}, 
			#{item.receiver_name}, #{item.receiver_zip}, #{item.receiver_addr1}, #{item.receiver_addr2}, #{item.receiver_tel}, #{item.receiver_phone}, #{item.receiver_msg}, 
			#{item.order_state}, #{item.order_pay}, #{item.product_num}, #{item.product_name}, #{item.product_thumbnail}, #{item.product_price}, #{item.product_sale}, 
			#{item.product_color}, #{item.product_size}, #{item.product_count}, #{item.user_id})
		</foreach>
	</insert>
	
</mapper>