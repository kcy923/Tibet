<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mappers.product">
	
	<select id="productDetail" resultType="com.myspring.tibet.product.vo.ProductVO">
		select 
		product_num, product_name, product_price, product_sale,
		substring_index(product_color,",",1) as color1,
		substring_index(substring_index(product_color,",",2),",",-1) as color2,
		substring_index(substring_index(product_color,",",3),",",-1) as color3,
		substring_index(product_size,",",1) as size1,
		substring_index(substring_index(product_size,",",2),",",-1) as size2,
		substring_index(substring_index(product_size,",",3),",",-1) as size3, 
		product_detail,product_thumbnail,product_img1,product_img2,product_img3,main_category_num, main_category_num 
		from product where product_num=#{product_num}
	</select>

	<select id="productListLowprice" resultType="com.myspring.tibet.product.vo.ProductVO">
		select product_num , product_name, product_price, product_sale, product_thumbnail,main_category_num, sub_category_num from product ORDER BY product_price
	</select>
	
	<select id="productListHighprice" resultType="com.myspring.tibet.product.vo.ProductVO">
		select product_num , product_name, product_price, product_sale, product_thumbnail,main_category_num, sub_category_num from product ORDER BY product_price DESC
	</select>
	
	<select id="productListNew" resultType="com.myspring.tibet.product.vo.ProductVO">
		select product_num , product_name, product_price, product_sale, product_thumbnail,product_date,main_category_num, sub_category_num from product ORDER BY product_date DESC
	</select>
	
	<select id="productListName" resultType="com.myspring.tibet.product.vo.ProductVO">
		select product_num , product_name, product_price, product_sale, product_thumbnail,product_date,main_category_num, sub_category_num from product ORDER BY product_name
	</select>
	
	<select id="mainCate" resultType="com.myspring.tibet.product.vo.MainCateVO">
		select * from main_category order by main_category_num
	</select>
	
</mapper>
